# Mock LLM mode — set to 'true' to bypass NIM API calls and return fake data
USE_MOCK_LLM=true

# GitHub personal access token (required for repo access)
GITHUB_TOKEN=

# NVIDIA NIM API key (required when USE_MOCK_LLM is not 'true')
NIM_API_KEY=
